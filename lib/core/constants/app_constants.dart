/// Global application constants.
/// Replace [kDevUserId] with a real value once Auth sprint is complete.
abstract final class AppConstants {
  /// Temporary hardcoded user ID used across all data layers
  /// until Authentication is implemented.
  static const String kDevUserId = 'user-123';
}
